import { Component, Input, Output } from '@angular/core';

@Component({
    moduleId : module.id,
    selector : "navbar",
    templateUrl : "./navbar.html"
    //template : ``
})
export class NavbarComponent{
    viewMode;
}